# Oxide — TODO

> ✅ **Round 5 done** — restart the dev server for the window glide + snap shortcut (main-process changes); the rest hot-reloads.

## Queue

*(empty — add new ideas here)*

## Done

- [x] ⌨️ **Archive keybind** — `Ctrl+E` archives all selected notes (also shown in the right-click menu)
- [x] 📥 **Default section setting** — Settings → General → "Default section" dropdown picks where captures & new notes land (same thing as right-click → "Set as capture target")
- [x] 🎨 **More section coloring** — the pastel now washes over the section's cards, count badge and caret too (subtle in both light & dark themes)
- [x] 📌 **Snap-to-location keybind** — `Ctrl+Shift+M` (rebindable) slides the panel to its home spot; pick left/right edge or any corner under Settings → Global shortcuts *(needs dev-server restart)*
- [x] 🛝 **Smooth window glide** — release a window drag while moving and it coasts to a stop with decaying momentum (drag itself stays native & lag-free) *(needs dev-server restart)*
- [x] 🖱️ **Allow deselecting** — click the selected note again (or any empty space) to clear the selection; `Esc` still works too
